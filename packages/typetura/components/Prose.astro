---
import { getClassesFromAttrs } from '../utils/utils';
const { ...attrs } = Astro.props;
const { classlist } = getClassesFromAttrs(attrs);
---

<div class:list={['typetura-prose', ...classlist]} {...attrs}>
	<slot />
</div>
